const general = require('./trivia/general.json') //todo-> Dynamic loading?

const CATEGORIES = [ "general" ]
const trivia = {
    general
}

function getRandomQuestion(category){
    if(category)

}
module.exports = {}